{% extends 'base.jinja' %}

{% block base %}
  <div class="container">
    <h3>제품 선택</h3>
    <ul class="list-group">
      {% for entry in entries %}
        <a class="list-group-item" href="{{ url_for('stats.graph', product=entry.id, count=7) }}">{{ entry.name }}</a>
      {% endfor %}
    </ul>
  </div>
{% endblock base %}
