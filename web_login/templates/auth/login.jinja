<div>

  <form action="{{ url_for('auth.login') }}" method="POST">
      <label>사용자 아이디</label>
      <div>
        <input id="user-id" name="user_id" type="text"/>
      </div>
    </div>
    
    <div>
      <label>사용자 비밀번호</label>
      <div>
        <input id="user-password" name="user_password" type="password"/>
      </div>
    </div>

    <div>
      <button type="submit">로그인</button>
    </div>
    
  </form>
</div>

